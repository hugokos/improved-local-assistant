# Dependency constraints - single source of truth for tricky libs
llama-index>=0.13,<0.14
llama-index-core>=0.13,<0.14
llama-index-embeddings-ollama>=0.7,<0.8

# Network graph - keep at current stable version
networkx==3.2.1

# Voice processing - exact versions for stability (Windows + Linux only)
sounddevice==0.4 ; platform_system != "Darwin"
webrtcvad==2.0.10 ; platform_system != "Darwin"
vosk==0.3.45 ; platform_system != "Darwin"

# Core framework - allow minor updates within range
fastapi>=0.110.0,<0.115.0
uvicorn>=0.27.0,<0.30.0
pydantic>=2.8.0,<3.0.0